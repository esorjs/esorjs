/**
 * Client - Native SSR Example
 *
 * Ultra-simple! Just import the component and it auto-hydrates.
 * The Declarative Shadow DOM is already in the HTML,
 * the component just needs to connect signals and handlers.
 */

import './counter.js';

console.log('✅ Native SSR component loaded!');
console.log('🎯 Declarative Shadow DOM auto-hydrated by browser');
console.log('⚡ Component connected signals and handlers automatically');
console.log('📦 Bundle: ~1 KB (native approach)');
